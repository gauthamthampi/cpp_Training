#include <iostream>
using namespace std;

/*
int arr() {
	int arr[3];
	cout << "Cap/size of arr: " << sizeof(arr) << endl;
	cout << " Address of arr[0] = " << &arr[0] << endl;
	cout << " Address of arr[0] = " << &arr[1] << endl;
	cout << " Address of arr[0] = " << &arr[2] << endl;

	//On a typical system, the memory addresses will be printed in a format that 
	// represents pointers, often looking like hexadecimal numbers (e.g., 0x7fff5fbff700).

	printf("Address of arr[0] = %u\n", &arr[0]);
	printf("Address of arr[1] = %u\n", &arr[1]);
	printf("Address of arr[2] = %u\n", &arr[2]);

	//%u is used to print the address as an unsigned integer. In modern C++, 
	// addresses are typically represented as pointers, and to print them correctly, you would 
	// generally use %p, but %u will work in many cases, though it's not ideal.
}

*/
/*
int main() {
	int arr[3] = { 101,202,303 }; //Declaration with initialization

	cout << "arr[0] = " << arr[0] << endl;
	cout << "arr[1] = " << arr[1] << endl;
	cout << "arr[2] = " << arr[2] << endl;
	//cout << "arr[3] = " << arr[3] << endl; //garbage value as arr out of bond

	for (int iv = 0;iv < 3;iv++) {
		cout << "arr[" << iv << "] = " << arr[iv] << endl;
	}

	return 0;
}
*/





